import 'package:flutter/material.dart';

class ViewDocumentsTab extends StatelessWidget {
  const ViewDocumentsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Hi muse")),
    );
  }
}
